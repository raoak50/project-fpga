lifNeuron works completely as expected...
It follows a Integrate -> Leak -> Fire order
Only has one i/o spike parameter (may need to update this at a later point)

completed lifSynapse module v1 which just acts as a singular connection between 2 neurons

completed lifNetwork topology so you can create as many neurons as you want
    -this was possible because of the generate and genvar
ran numerous test benches to check for errors

completed a interface for the fpga to make the project slightly interactable
